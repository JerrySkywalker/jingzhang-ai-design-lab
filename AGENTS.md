# Agent Working Agreement

This repository is the persistent **Program/Control Plane, design-memory and research workspace** for the Centennial Jing-Zhang AI Innovation Belt project. It is not the final submission repository.

## 1. Source-of-truth hierarchy

When statements conflict, use this order:

1. latest canonical files in `open-city-ai/haidian` for official rules/tooling;
2. `docs/programs/JZ-FINAL100-AWARD-CONSOLIDATION.md` for active Program sequencing/governance;
3. `docs/JINGZHANG_IN_PLACE_FINAL100_DESIGN_DOCTRINE.md` for Owner-approved final design doctrine;
4. `state/JZ100_PROGRAM_STATE.json` for current machine state;
5. official or cleared public primary sources;
6. reproducible derived calculations;
7. explicitly labelled assumptions/concepts.

Historical JZ97 files remain evidence but are superseded as active admission control.

Never upgrade an assumption, provisional geometry, benchmark claim, P0 reference dimension, generated idea or ROM estimate into official fact.

## 2. Current project state

- Owner-selected submission: `JINGZHANG_IN_PLACE`
- Project name: `京张续城 / Jing-Zhang In Place`
- Accepted official PR: `open-city-ai/haidian#4270`
- Accepted reviewed head: `4cb6b1bd7407781d5121fc51ce81e6c8a06c1de1`
- Accepted merge commit: `e0927ba94154e3bf3b66c720f61560b9adba877b`
- Trusted score: `93/100`
- Trusted vector: `5,5,5,4,5,5,4`
- Active Program: `JZ-FINAL100-AWARD-CONSOLIDATION-0830`
- Current Goal: `JZ100-G01-FINAL100-AWARD-CONSOLIDATION-001`
- Machine target: `100/100`
- Human-panel target: `award-candidate`
- Competition/award/government adoption remains undetermined; never imply otherwise.

## 3. Mandatory Goal bootstrap

Before executing the final JZ100 Goal, read:

1. `docs/programs/JZ-FINAL100-AWARD-CONSOLIDATION.md`
2. `docs/JINGZHANG_IN_PLACE_FINAL100_DESIGN_DOCTRINE.md`
3. `docs/CURRENT_PROGRAM.md`
4. `state/JZ100_PROGRAM_STATE.json`
5. `docs/PROGRAM_CONTROL_AIRLOCK.md`
6. `goals/JZ100-G01-FINAL100-AWARD-CONSOLIDATION-001.md`

Do not block on the old JZ97 C1/C2/C3 gates. They belong to a superseded historical Program.

## 4. Execution policy

Preferred final product implementer: **Terra Ultra**, sole writer.

Known role-wrapper entry point:

`jcodex-role -Role implementer -WorkingDirectory 'V:\src\haidian'`

Use the local model/role configuration selecting Terra Ultra for the implementer role.

Sol Ultra is appropriate as:

- read-only architect/Supervisor; or
- an explicitly configured complete single-writer substitute.

Never run Terra and Sol as simultaneous writers on the same product branch.

The final Goal is a long unattended train. Owner is unavailable for routine implementation decisions. The agent should continue through recoverable rendering/test/review failures without asking for routine confirmation.

Stop only for hard conditions defined by the Goal: unavailable credentials/auth, destructive/irreversible external action outside the approved PR, material official-rule contradiction that cannot be reconciled, an unsafe truth claim that cannot be downgraded, or an external system/deadline that makes further progress impossible.

## 5. Owner-approved final design doctrine

### Public identity

- Project: **京张续城 / Jing-Zhang In Place**
- Slogan: **让未来生长，让城市相续。**
- Humanistic thesis: **技术属于时代，城市属于世代。**
- City philosophy: **城市不是技术的容器，而是代际生活的连续体。**

### Human-facing design structure

- Continuities: **续时 / 续生 / 续忆**
- Temporal layers: **长久城市 / 可适应城市 / 暂居智能**
- Key-area narrative: **验证 / 共处 / 裁决**

The first human impression must be urban-design-first, not audit/scoring/compliance-first.

## 6. Protected score/design dimensions

Freeze the five dimensions already trusted at 5/5:

- Brief Alignment
- Originality
- AI Planning Innovation
- Public Interest / Inclusion
- Risk / Compliance

Preserve:

`heterogeneous existing city -> STATUS × ACTION -> ordinary-space sufficiency -> AI spatial admission -> 12→3→9 -> minimum reversible spatial delta -> human authority/stop/reset -> ordinary city`

Also preserve S01/S04/S07 selection, nine NO-BUILD/ordinary outcomes, G0–G4, 15 renewal project families, five conditional regional interfaces and evidence-state discipline.

Do not replace complete urban design with robotics, sensing, governance, simulation or agent tooling. Avoid generic smart-city drift and rubric-driven document bloat.

## 7. Final score-lift discipline

Only two participant-controlled lifts are admitted:

1. `implementation_feasibility 4→5`: one integrated S01 participant-authored P0 pre-feasibility bridge.
2. `expression_completeness 4→5`: identity, cover, narrative hierarchy, spatial typologies, A0/HTML recomposition and rendered visual QA.

Do not add new scenarios/personas/project counts/partnership claims merely to chase points.

## 8. S01 P0 truth boundary

S01 is the only deep P0.

A `12 m × 9 m` zero-coordinate envelope may appear only as `DESIGN_ASSUMPTION` for participant layout/quantity/jury communication. It is not surveyed, approved, official or field-verified.

Capacity is an operational design assumption, not approved occupancy. Staffing is per-session role classes, not named organizations/FTE commitments. Costs are source-bounded ROM/sensitivity only; `market_quotation_count=0`, `approved_budget=null`, `funding_commitment=null` remain explicit.

All real site/professional/safety/accessibility/legal/operations/procurement gates remain `HOLD_EXTERNAL`; `P0_GO=false`.

## 9. Human visual quality is a hard gate

Official `visual_review.py` structural PASS does not prove visual quality.

The final Goal must actually render and inspect gallery card, bilingual HTML desktop/mobile, all changed core figures and every page of A0/A3 in both languages.

Hard receipt:

`URBAN_DESIGN_FIRST_IMPRESSION=PASS`

Also require:

- `HUMANISTIC_THESIS_VISIBLE=PASS`
- `P0_PROFESSIONAL_HANDOFF=PASS`
- `AUDIT_LANGUAGE_SUBORDINATE=PASS`

## 10. Repository boundary

Follow `docs/PROGRAM_CONTROL_AIRLOCK.md`.

- This repository may contain doctrine, roadmaps, goals, state, benchmark/calibration, decisions, experiments and run receipts.
- `JerrySkywalker/haidian` is product-only.
- Program/control files must never appear in the official participant PR diff.
- Do not mirror the full official repository or commit competitors' large media/PDFs here.
- Keep credentials, private data and proprietary planning material out of this public repository.

## 11. Change discipline

For the final train:

1. fresh-fetch official upstream at start;
2. create one new product branch from then-current upstream/main;
3. finish source edits before formal validation;
4. minimize rebuild churn;
5. perform real rendered human visual QA;
6. run compressed contributor certification;
7. run one independent read-only Supervisor on frozen worktree;
8. open one final successor PR;
9. use only reasoned content-changing same-PR trusted-review repair cycles; no identical-head rerolls.

The accepted 93-point official version remains the protected historical anchor.

## 12. Scoring and award discipline

- Official-style dimensions are integer 0..5.
- Local adviser estimates are not official scores.
- Official trusted score exists only when the official maintainer pipeline reviews an exact head.
- Program success requires Trusted Review Agent `100/100`, but 100 is not an award result.
- Formal human panel/award result remains external; the Program may target `award-candidate` but must never claim it before it exists.

## 13. Historical program memory

`JZ-97-CONVERGENCE-TRAIN-001`, its state, Goals, calibration plans and run evidence are historical and must remain available. They are no longer prerequisites for the final 2026-08-30 consolidation train.
