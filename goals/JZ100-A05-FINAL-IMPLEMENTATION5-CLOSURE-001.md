# JZ100 A05 — Final Implementation-5 Closure Train

**Goal ID:** `JZ100-A05-FINAL-IMPLEMENTATION5-CLOSURE-001`  
**Program:** `JZ-FINAL100-AWARD-CONSOLIDATION-0830`  
**Mode:** one final continuous unattended product train  
**Preferred writer:** Terra Ultra, sole writer  
**Target active work:** 8–12 hours  
**Machine target:** `100/100`, vector `5,5,5,5,5,5,5`  
**Submission completion:** open one successor PR automatically; after an exact-head 100/100, freeze the head and complete the normal submission/merge path without further content changes.

## 1. Mission

Starting from fresh current `open-city-ai/haidian` `upstream/main`, produce exactly one truthful, implementation-only successor to the accepted **京张续城 / Jing-Zhang In Place** PR #4311.

Current accepted baseline:

- PR: `open-city-ai/haidian#4311`
- reviewed exact head: `87a15cee9e0e86670db340bcb19f54caff6cb815`
- merge commit: `f68ba15a5b83192508ba8fc5c8a1be879f101e50`
- trusted score: `96/100`
- vector: `5,5,5,4,5,5,5`
- status: `formal-review-ready`, `featured-candidate`, no blockers

**Only Implementation Feasibility may be changed for score lift.** The six dimensions already at 5/5 are frozen and must not regress.

The A04 forensic audit found three qualifying participant-controlled deltas. Implement those deltas completely, visibly and professionally; do not broaden into another general design iteration.

## 2. Frozen six 5/5 dimensions

Protect:

1. Brief Alignment = 5
2. Originality = 5
3. AI Planning Innovation = 5
4. Public Interest / Inclusion = 5
5. Risk / Compliance = 5
6. Expression Completeness = 5

Preserve without redesign:

- 京张续城 / Jing-Zhang In Place
- 让未来生长，让城市相续。
- 技术属于时代，城市属于世代。
- 城市不是技术的容器，而是代际生活的连续体。
- 续时 / 续生 / 续忆
- 长久城市 / 可适应城市 / 暂居智能
- 众智园=验证 / AI原点=共处 / 大钟寺=裁决
- STATUS × ACTION
- AI_OFF_CITY
- ORDINARY_SPACE_SUFFICIENT
- 12→3→9
- S01/S04/S07
- nine ordinary-space / NO-BUILD outcomes
- human final authority
- STOP / RESET
- G0–G4
- 15 project families
- five conditional regional interfaces
- current visual identity, cover and jury hierarchy unless a direct synchronization defect requires a minimal repair

Do not introduce new scenarios, personas, project counts, key-area topology, branding systems or generic smart-city content.

## 3. Truth boundary — absolute

Maintain throughout:

- `P0_GO=false`
- all real site/rights/operator/professional/engineering/field/approval gates remain `HOLD_EXTERNAL` until real evidence exists
- `market_quotation_count=0`
- `approved_budget=null`
- `funding_commitment=null`
- no appointed operator claim
- no rights-holder consent claim
- no engineering capacity claim
- no field-performance claim
- no approval claim
- no government/railway/university/company partnership claim

Every new planning construct must be labelled `PARTICIPANT_REFERENCE`, `DESIGN_ASSUMPTION`, `RELATIVE_SENSITIVITY`, or an equally explicit non-official state.

A 100 score must come from completing the participant-controlled pre-feasibility package, never from upgrading external reality.

## 4. Qualifying Delta 1 — reviewer-visible participant-reference quantification

Create a compact, professional quantitative delivery closure for the existing S01 P0 and implementation chain.

### 4.1 Unpriced quantities

Create or extend the existing canonical readiness/P0 structure with an **unpriced quantity schedule** tied to the existing eight removable/reusable component classes and required implementation support items.

For each lot/item include, where truthful:

- item/lot id
- function
- reference unit
- participant-reference quantity or quantity rule
- low/reference/high quantity sensitivity where uncertainty matters
- dependency
- reusable/removable state
- procurement/acceptance gate
- exclusion boundary

Do not invent product models, market quotations or site-specific quantities that require a selected carrier.

### 4.2 Staffing / role-hour basis

Add a participant-reference session staffing model using existing role classes.

Expose:

- setup role-hours
- session role-hours
- closeout/reset role-hours
- inspection/review role-hours
- maintenance/storage role-hours or cadence basis
- low/reference/high activity sensitivity

If an FTE-equivalent is useful, it must be explicitly a planning conversion from role-hours, not an employment or operator commitment.

### 4.3 Relative CAPEX / OPEX sensitivity

Do **not** fabricate CNY totals.

Unless genuinely applicable, scope/date/location-matched public unit rates can be verified, use a normalized relative reference model such as:

- reference case index = `1.00`
- low / reference / high indices
- cost-driver shares/ranges
- quantity contribution
- setup/inspection contribution
- storage/transport contribution
- maintenance/replacement contribution
- professional/external items explicitly excluded or kept external

The model must distinguish:

- participant-reference delivery sensitivity
- future market quotation
- approved budget
- funding commitment

The latter three remain absent/null.

### 4.4 Lifecycle and restoration reserve

Add an **unfunded restoration/exit reserve method** covering future removal/restoration risk without claiming funding.

Express as:

- reserve driver(s)
- relative sensitivity / allowance rule
- trigger for professional recalculation
- explicit `funded=false` / no commitment

Include removal, reverse-reset, surface/route restoration, component quarantine/replacement and archive/closeout where applicable.

## 5. Qualifying Delta 2 — reviewer-visible conditional delivery control

### 5.1 Dependency programme

Create a conditional implementation programme whose actual calendar dates remain null until external authorization.

Use stages or dependency sequence such as:

`participant package complete -> G1 external verification -> professional closure -> carrier/task release -> procurement/assembly -> commissioning rehearsal -> conditional P0 session -> reset/restoration -> acceptance/closeout`

For every stage include:

- predecessor
- role-type recipient
- required artifact
- participant-checkable entry condition
- external entry condition
- output
- fail/hold fallback
- actual_start=null
- actual_finish=null

Do not manufacture a project schedule.

### 5.2 Gate-addressed handoff recipients

Convert existing role prose into a compact recipient/artifact/acceptance map by **role type**, not named organization.

Include relevant classes such as:

- rights/access verifier
- planning/urban-design professional
- architecture/public-space professional
- structure/fire/electrical/accessibility/privacy or other applicable professional reviewer
- test operator candidate
- human safety authority candidate
- maintenance/reset role
- independent reviewer
- procurement/contracting role where applicable

No appointment is implied.

### 5.3 Discipline and competence checklist

For each future professional/operational role, state:

- competence/discipline needed
- evidence to be checked
- what the participant package can verify now
- what remains external
- NO-GO if absent

Do not invent certifications or license numbers.

### 5.4 Commissioning / inspection / maintenance cadence

Make existing generic readiness logic measurable without pretending field completion.

Define participant-reference cadence classes such as:

- before deployment
- before every session
- after every session
- after stop/fault
- periodic storage/inventory review
- future manufacturer/professional interval = null until selection

Tie each cadence to:

- checklist owner by role type
- record produced
- stop/quarantine condition
- acceptance destination

### 5.5 Procurement path

Create an **unpriced participant-reference procurement path**:

- lot/package structure
- specification freeze gate
- professional acceptance prerequisite
- request-for-quotation gate
- vendor/market evidence future state
- inspection/acceptance gate
- payment/funding explicitly outside current evidence
- fallback to lower-tech / NO-BUILD where appropriate

Keep `market_quotation_count=0`.

### 5.6 Measurable layered acceptance ledger

Upgrade the current qualitative acceptance categories into a compact ledger with measurable states.

Split at minimum:

- participant-checkable now
- professional verification required
- field verification required
- external authorization required

For every item include:

- indicator/check
- expected evidence type
- current state
- responsible role type
- pass/fail/HOLD rule
- release consequence

Do not mark professional/field/external checks passed.

## 6. Qualifying Delta 3 — positive bounded release semantics

Only after Sections 4 and 5 are materially complete, add and consistently expose the following participant-scope state:

- `PARTICIPANT_PRE_FEASIBILITY_PACKAGE_COMPLETE=true`
- `PROFESSIONAL_HANDOFF_COMPLETE=true`
- `READY_FOR_G1_EXTERNAL_VERIFICATION=true`
- `NEXT_DECISION_PACKAGE_COMPLETE=true`

These statements mean **only** that the participant-authored package is complete for the next external verification stage.

They must be paired visibly with:

- `P0_GO=false`
- `EXTERNAL_RELEASES_OPEN=false`
- rights/access = HOLD
- operator appointment = HOLD
- engineering/professional acceptance = HOLD
- budget/funding = null
- quotations = 0
- field performance = null
- approval = HOLD

Never use `field-ready`, `ready-to-build`, `approved`, `funded`, `commissioned`, `operational`, or equivalent language.

## 7. Reviewer-visible surface contract

A05 is invalid if it only adds hidden JSON.

Material implementation evidence must be synchronized into the surfaces the current reviewer actually receives.

Required reviewer-visible channels:

### Raw/canonical scoring surfaces

- `proposal.md`
- `metrics.json`
- `assumptions.json`
- `sources.json` only where new source/provenance is genuinely needed
- `compliance_matrix.json`
- `design_depth_matrix.json`
- other current raw files actually supplied by the current reviewer pipeline, as verified from fresh upstream tooling

### Human + fixed visual surfaces

- existing bilingual `metrics-evidence` figure pair, or another already-fixed reviewer figure only if the current reviewer contract actually supplies it
- first A0 page in both languages
- first A3 page in both languages
- initial 1440×1600 report/visual HTML viewports in both languages
- full synchronized A0/A3/report/visual deliverables where source changes require regeneration

The first reviewer-visible surfaces should communicate, in one glance:

1. participant package complete;
2. quantities / staffing / lifecycle sensitivity exist;
3. conditional programme and measurable acceptance exist;
4. external releases remain HOLD;
5. P0 remains NO-GO until real evidence closes the gates.

Do not create score-only montages or manipulate reviewer code.

## 8. Human professional quality

Although this is an implementation-only sprint, every change must improve a real professional handoff.

A competent implementation expert should be able to answer from the package:

- What would be procured, in what unpriced quantity structure?
- What staffing effort does one reference session require?
- What drives CAPEX/OPEX/lifecycle sensitivity?
- What reserve is needed for removal/restoration, without claiming funding?
- What sequence happens next?
- Which role type receives which artifact?
- Which discipline checks what?
- What is checked before/after a session?
- What constitutes package acceptance versus professional/field/external acceptance?
- What happens when any gate fails?
- Why is the package complete while the project remains `P0_GO=false`?

If the answer is not obvious, the sprint is not complete.

## 9. Expected bounded file scope

Expected changes: approximately 18–22 files inside only:

`submissions/JerrySkywalker/jingzhang-in-place/**`

Expected substantive sources:

- `proposal.md`
- `proposal.en.md`
- `metrics.json`
- `assumptions.json`
- `compliance_matrix.json`
- `design_depth_matrix.json`
- existing P0/readiness structured assets
- optional one compact implementation-quantification asset if it reduces duplication
- bilingual `metrics-evidence` figures
- bilingual report HTML
- bilingual visual HTML
- four synchronized A0/A3 PDFs
- changelog
- manifest
- self-check
- copyright/provenance only if genuinely affected

Avoid unless required by a direct defect:

- all GeoJSON
- cover/identity redesign
- site-overview / key-area / mobility conceptual redesign
- scenarios/personas/cases/landmarks counts
- regional-interface semantics
- repository scripts/rubric/workflows
- any other submission
- control-plane files inside the product PR

## 10. Admission and branch

At execution start:

1. fresh-fetch `upstream/main` and `origin`;
2. identify current official rules/reviewer tooling from fresh upstream;
3. prove PR #4311 merge is an ancestor of current upstream/main;
4. prove Jerry's current submission subtree still contains the accepted #4311 package or identify any later legitimate Jerry mutation;
5. create one new successor branch from **then-current** `upstream/main`;
6. do not build on the old A03 branch;
7. enforce submission-subtree-only write boundary.

Suggested branch:

`submission/JerrySkywalker/jingzhang-in-place-jz100-a05-final-0831`

If that branch already exists, choose a unique bounded suffix rather than force-resetting it.

## 11. Build / rendered QA

Minimize churn:

1. complete source/structured-data changes;
2. regenerate affected bilingual figures/HTML/PDFs;
3. render and inspect:
   - report zh/en desktop and narrow/mobile
   - visual zh/en desktop and narrow/mobile
   - A0 all pages zh/en
   - A3 all pages zh/en
   - every changed fixed reviewer figure
4. check clipping, tofu, small text, table density, legend obstruction, hierarchy, bilingual equivalence and first-viewport/first-page readability;
5. batch repair visual defects;
6. rebuild only as necessary.

Expression is already 5. Do not degrade it while adding implementation density.

## 12. Certification

After final content bytes are frozen, use the **current upstream tooling**, not stale copied commands if interfaces changed.

Expected fast path remains conceptually:

1. refresh submission manifest;
2. marked full self-check;
3. participant preflight with self-check reuse and push dry run;
4. `git diff --check`;
5. prove outside-scope files = 0;
6. inspect final rendered evidence;
7. one independent read-only Supervisor on the frozen exact head before PR publication when practical.

If any file changes after marked certification, recertify before push.

## 13. Successor PR

Create exactly one successor PR automatically after local certification.

Suggested title:

`Jing-Zhang In Place — complete quantitative pre-feasibility handoff`

The PR body must describe professional implementation closure, not score hacking.

Include:

- current #4311 accepted baseline;
- unpriced quantity/staffing/sensitivity closure;
- conditional programme and measurable acceptance;
- participant-complete / external-HOLD split;
- truth boundaries;
- local certification receipts;
- explicit request to bind CI and Trusted Review to exact head;
- no request for identical-head reroll.

## 14. Exact-head trusted review loop

Wait for official exact-head CI and Trusted Review.

### If exact-head score = 100 and vector = 5,5,5,5,5,5,5

1. Freeze immediately.
2. Make no cosmetic/content changes.
3. Record exact reviewed head, package hash, CI run and review URL/id.
4. Ensure PR is ready for review and not draft.
5. Proceed to **Section 15 Submission Completion** automatically.

### If score < 100 because one of the frozen six dimensions regressed

Repair the regression first on the same PR if the cause is participant-controlled, recertify, push a new head and wait for a new exact-head review.

### If Implementation remains 4

Read the exact new review carefully.

A content-changing repair is permitted only when the reviewer identifies a **specific participant-controlled defect** that:

- is inside this A05 implementation-only scope;
- improves real professional quality;
- does not fabricate external reality;
- does not regress a frozen dimension.

Maximum same-PR content-changing repair cycles after the initial reviewed head: **2**.

No identical-head rerolls.

If after implementing all three A04 qualifying deltas the reviewer still withholds Implementation 5 solely for external rights/operator/engineering/cost/funding/field/approval facts, terminate `BLOCKED_EXTERNAL` rather than invent evidence or inflate the package further.

## 15. Submission Completion after 100

The Owner explicitly authorizes the normal final submission/merge completion path **only after** all of the following are true on the same exact head:

- Trusted Review Agent = `100/100`
- vector = `5,5,5,5,5,5,5`
- official exact-head CI = PASS
- no required repair
- six protected dimensions = 5
- PR head has not moved since the review

Then:

1. Freeze the exact 100-point head.
2. If the official maintainer workflow has already merged the PR, verify the merge and continue.
3. If the PR remains open, first use the repository's normal participant submission path and wait for maintainer/automation merge.
4. If and only if the authenticated participant has ordinary merge permission **and** current repository instructions/policy permit participant self-merge, the Owner authorizes a normal non-admin/non-bypass merge of this exact 100-point head.
5. Never force-push, bypass protections, use admin override, rewrite history, or merge a head that differs from the reviewed 100-point SHA.
6. If participant merge permission/policy is absent, do not treat that as a product failure: wait for external maintainer merge and monitor it.
7. After merge, verify current official main contains the exact reviewed submission tree/package and record merge SHA.

This section is the only merge authorization in A05. No merge is authorized before 100/100.

## 16. Final Supervisor

After the terminal trusted outcome and, when applicable, merge completion, launch one independent read-only Supervisor (prefer Sol Ultra) to verify:

- exact trusted reviewed SHA;
- exact score/vector;
- exact CI status;
- PR/merge state;
- submission subtree integrity;
- all six protected dimensions remain 5;
- no external HOLD was falsely closed;
- no unreviewed post-100 content change exists.

Do not use the Supervisor to initiate redesign.

## 17. Terminal states

### COMPLETE_100_SUBMITTED

Use only when:

- 100/100 exact-head trusted review confirmed;
- CI PASS;
- exact 100-point head frozen;
- normal submission path completed to the maximum permitted extent;
- if merged, official main verified;
- final Supervisor PASS.

### COMPLETE_100_AWAITING_EXTERNAL_MERGE

Use when 100/100 and CI are confirmed and the exact head is frozen, but only a maintainer can merge and the PR remains open. Continue monitoring until the external merge occurs while the process remains available; do not mutate the head.

### BLOCKED_EXTERNAL

Use only when all A05 participant-controlled closure is complete and a fresh exact review still withholds Implementation 5 solely for external reality that cannot truthfully be created.

### BLOCKED_INFRASTRUCTURE

Use for unrecoverable credentials/network/repository/deadline failure.

## 18. Final owner receipt

End with:

```text
DISPOSITION=
PROGRAM_ID=JZ-FINAL100-AWARD-CONSOLIDATION-0830
GOAL_ID=JZ100-A05-FINAL-IMPLEMENTATION5-CLOSURE-001

START_UPSTREAM=
FINAL_UPSTREAM_BASE=
FINAL_BRANCH=
FINAL_HEAD=
PR_NUMBER=
PR_URL=

BASELINE_PR4311_PRESENT=true/false
PROTECTED_SIX_DIMENSIONS_REGRESSED=true/false

QD1_QUANTIFICATION_COMPLETE=
QD2_DELIVERY_CONTROL_COMPLETE=
QD3_POSITIVE_RELEASE_SEMANTICS_COMPLETE=
UNPRICED_QUANTITIES=
STAFFING_ROLE_HOURS=
CAPEX_OPEX_RELATIVE_SENSITIVITY=
RESTORATION_RESERVE_METHOD=
CONDITIONAL_PROGRAMME=
PROCUREMENT_PATH=
MEASURABLE_ACCEPTANCE_LEDGER=
PARTICIPANT_PRE_FEASIBILITY_PACKAGE_COMPLETE=
PROFESSIONAL_HANDOFF_COMPLETE=
READY_FOR_G1_EXTERNAL_VERIFICATION=
NEXT_DECISION_PACKAGE_COMPLETE=

P0_GO=false
MARKET_QUOTATION_COUNT=0
APPROVED_BUDGET=null
FUNDING_COMMITMENT=null
EXTERNAL_HOLDS_PRESERVED=true

DETERMINISTIC=
SPATIAL=
VISUAL=
PROFESSIONAL=
PARTICIPANT_PREFLIGHT=
OUTSIDE_SCOPE_FILES=
GIT_DIFF_CHECK=
RENDERED_BILINGUAL_QA=
REVIEWER_VISIBLE_IMPLEMENTATION_CLOSURE=

REMOTE_CI=
TRUSTED_REVIEW_SCORE=
TRUSTED_VECTOR=
TRUSTED_DISPOSITION=
TRUSTED_REVIEWED_HEAD=
TRUSTED_REPAIR_CYCLES=

MERGE_AUTHORIZED_ONLY_AFTER_100=true
MERGED=
MERGE_SHA=
OFFICIAL_MAIN_SUBMISSION_TREE_VERIFIED=
FINAL_SUPERVISOR=

OWNER_ACTION_REQUIRED=
```

Proceed as one continuous final train. The objective is an exact-head 100/100 professional submission, not a larger package. Stop only at a truthful terminal state.
