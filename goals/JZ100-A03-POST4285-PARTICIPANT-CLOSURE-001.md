# JZ100 A03 — Post-#4285 Participant Closure Overnight Train

**Goal ID:** `JZ100-A03-POST4285-PARTICIPANT-CLOSURE-001`  
**Program:** `JZ-FINAL100-AWARD-CONSOLIDATION-0830`  
**Mode:** one continuous unattended product train  
**Preferred writer:** Terra Ultra, sole writer  
**Product workdir:** `V:\src\haidian`  
**Target unattended window:** 6–8 hours  

## 1. Mission

Starting from the latest official `open-city-ai/haidian` main, create exactly one truthful successor PR for **京张续城 / Jing-Zhang In Place** that maximizes the probability of moving the current exact trusted vector from `5,5,5,4,5,5,4` to `5,5,5,5,5,5,5` / **100/100**, while further improving the package for later human professional judging.

The current merged baseline is PR `open-city-ai/haidian#4285`, reviewed exact head `af75ac57f97f977f5f842dcf28a8b3263fb07e9a`, merge `17787b8581cc2f4b79f111277ae1930413673d67`, trusted score `93/100`, `formal-review-ready`, `featured-candidate`, no blockers.

This Goal implements the read-only audit `JZ100-A02-POST4285-MACHINE100-GAP-AUDIT-0831` Strategy B: **implementation readiness + reviewer visibility**.

Do not stop for routine Owner confirmation. Do not fabricate reality. Do not merge the PR yourself.

## 2. Why this train is authorized

The A02 audit traced the current trusted-review pipeline and found:

- there is **no hard external-evidence requirement** for Implementation 5;
- there is **no explicit score cap** caused by provisional/HOLD status;
- a current same-pipeline 100-point precedent achieved Implementation 5 while site authority, operator, budget, quotations, professional sign-off and field observations were still unconfirmed/HOLD;
- that precedent's distinguishing advantage was a much more complete participant-authored delivery/readiness system;
- Expression 5 has many current precedents and the remaining deductions in #4285 are bounded density/visibility problems.

This evidence makes a truthful 100 path credible but not guaranteed.

## 3. Protected design doctrine — do not redesign

Preserve all five trusted 5/5 dimensions and the Owner-approved doctrine:

- 京张续城 / Jing-Zhang In Place
- 让未来生长，让城市相续。
- 技术属于时代，城市属于世代。
- 城市不是技术的容器，而是代际生活的连续体。
- 续时 / 续生 / 续忆
- 长久城市 / 可适应城市 / 暂居智能
- 众智园=验证 / AI原点=共处 / 大钟寺=裁决
- STATUS × ACTION
- AI_OFF_CITY
- ORDINARY_SPACE_SUFFICIENT
- 12→3→9
- S01/S04/S07
- nine NO-BUILD / ordinary-carrier outcomes
- human final authority
- STOP / RESET
- G0–G4
- 15 project families
- five conditional regional interfaces
- public-interest floor and evidence-state discipline

Do not copy benchmark concepts, wording, values, topology, identity, cost numbers or layouts.

## 4. Truth boundary

All external facts that remain unconfirmed MUST stay unconfirmed/HOLD, including:

- actual site/carrier rights and access;
- appointed operator/task owner/statutory or professional authority;
- field capacity, accessibility, structure, fire, electrical, utilities or maintenance evidence;
- authorized budget, procurement, quotations or funding commitment;
- actual equipment/data/task performance;
- commissioning or field acceptance;
- government, railway, university, company or partnership support.

Keep at minimum:

- `P0_GO=false`
- `market_quotation_count=0`
- `approved_budget=null`
- `funding_commitment=null`
- external gates=`HOLD_EXTERNAL`

Every new planning construct that is not an observed/official fact must be visibly labelled `PARTICIPANT_REFERENCE`, `DESIGN_ASSUMPTION`, `CONCEPT`, `PUBLIC_CONTEXT_ONLY`, or another existing truthful evidence state.

## 5. Fresh admission

Before writing product files:

1. fetch `upstream/main` and `origin` fresh;
2. verify PR #4285 merge is an ancestor of current upstream main;
3. verify Jerry's current submission subtree equals or cleanly descends from the #4285 merged subtree and has not been unexpectedly changed later;
4. read CURRENT upstream contributor/reviewer tooling and rules;
5. create one new branch from current upstream/main, recommended:
   `submission/JerrySkywalker/jingzhang-in-place-jz100-a03-0831`;
6. enforce a submission-subtree-only allowlist;
7. never build from the stale local `final96` branch.

If the current official rules materially contradict this Goal, stop and report the exact contradiction. Ordinary upstream advancement is not a blocker.

## 6. Reviewer visibility contract — critical

The current Trusted Review Agent receives raw structured evidence from the canonical proposal/manifest/metrics/assumptions/sources/self-check/compliance/standard/design-depth surfaces plus central trusted reports, and exactly the fixed visual set produced by current upstream tooling.

The A02 audit found that custom assets such as `p0-s01-reference.json`, `renewal-project-portfolio.json`, `taskbook-closure.json` and `design-evidence-index.json` are **not** automatically supplied as raw model evidence. The reviewer also sees only page one of each A0/A3 PDF, not A3 pages 2–4.

Therefore:

- do not solve Implementation 5 by adding hidden JSON alone;
- synchronize every material readiness improvement into reviewer-visible raw surfaces (`proposal`, `assumptions`, `sources`, `compliance`, `design-depth`, metrics only when appropriate);
- make the integrated readiness logic visible in a genuinely useful fixed reviewer figure, preferably the existing `metrics-evidence` pair, and in the initial viewport of human-facing HTML where appropriate;
- make A0 page 1 and A3 page 1 self-sufficient, not index-only;
- do not modify reviewer code/workflows or create score-only montage artifacts.

## 7. WP-A — Integrated participant readiness dossier

Create **one integrated readiness dossier**, not ten disconnected appendices. A single structured source artifact may be added if useful, e.g. `visual/assets/implementation-readiness-dossier.json`, but its essential contents must be synchronized into reviewer-visible surfaces.

The dossier must join these participant-controlled components:

### A1. Carrier decision register

Define a site-independent carrier screening and rejection workflow:

- candidate carrier requirements;
- ordinary-route preservation;
- access/rights evidence required;
- minimum services/interfaces;
- disqualifiers;
- fallback to lower-tech or NO-BUILD;
- who verifies each item later;
- current candidate/rights facts remain UNKNOWN/HOLD.

This is a decision method, not a claim that a carrier exists.

### A2. Bounded `PARTICIPANT_REFERENCE` task packet

For S01 define a reference mission sufficiently concrete for professional handoff without pretending a real commissioned task exists:

- purpose;
- inputs;
- outputs;
- equipment boundary/categories;
- data boundary;
- task-owner requirements;
- operator/safety roles;
- success/failure categories;
- stop conditions;
- reset evidence;
- explicit `actual_task_owner=null`, field outcome null.

Keep the existing 12 m × 9 m zero-coordinate envelope only as `DESIGN_ASSUMPTION`.

### A3. Role-type RACI / handoff matrix

For each next-stage action identify role TYPE, recipient, artifact, decision, sign-off class and fallback. Do not name an organization unless an official public source supports a context-only fact and the text explicitly says it is not appointment/authorization.

Cover at least:

- owner/rights verification;
- planning/site professional;
- accessibility;
- fire/life safety;
- structural/load/anchorage;
- electrical/isolation;
- privacy/data/legal;
- operations/safety competence;
- procurement/cost classification;
- independent acceptance/reset witness.

### A4. Hazard / lockout / NO-GO matrix

Create a generic pre-feasibility safety case with hazard classes and ownership of future verification. It must include human stop, lockout/isolation, controlled clearance, quarantine, commissioning prerequisites and NO-GO conditions.

Do not invent code clearances, engineering performance, response times, loads, electrical values or approval outcomes.

### A5. Procurement / cost boundary register

Strengthen cost-readiness without inventing prices:

- component quantities/categories where defensible;
- inclusion/exclusion boundary;
- uncertainty classification;
- CAPEX/OPEX formula structure;
- role-hours/setup/reset/inspection/storage/transport/consumables/replacement categories;
- monetary public rates only if applicability is verified from authoritative/current sources;
- otherwise use null or relative sensitivity.

Keep quotation/budget/funding fields null as required.

### A6. Operations runbook

Make the participant-operable session lifecycle inspectable:

- booking/scheduling assumption;
- role qualification requirement classes;
- pre-open checks;
- setup;
- ordinary-baseline verification;
- stop rehearsal;
- one controlled task;
- incident/deviation log;
- shutdown;
- isolation;
- reset/inventory;
- maintenance/quarantine;
- closeout/archival;
- no unsupported duration claims.

### A7. Layered acceptance / commissioning matrix

Consolidate:

- participant-checkable-now;
- professional-check-required;
- field-observation-required;
- pass/fail/hold decision;
- artifact/evidence produced;
- fallback/NO-BUILD consequence.

### A8. Rights / authority acquisition workflow

Do not claim rights. Show how a future team would acquire and verify them:

- identify legal/rightsholder class;
- request evidence;
- validate scope/time/use;
- define authorized signatory/decision requirement;
- record expiry/revocation;
- stop and NO-BUILD on unresolved authority.

### A9. Public-context refresh

Research only current authoritative public sources that materially clarify CONTEXT. Candidate opportunities identified by A02 include official corridor planning status, park/open-space context, railway-land historical process and Dazhongsi transport context.

Every such source must carry:

- as-of date;
- precise supported proposition;
- `does_not_support` fields for rights/operator/budget/task/engineering/approval as applicable.

Do not use public context to imply present permission.

### A10. Day-1 / Week-1 HOLD closure map

Write a professional G1 start plan:

- what the future team does on Day 1;
- what it measures/checks during Week 1;
- which HOLD each action can close;
- what evidence artifact results;
- which failure chooses lower-tech or NO-BUILD.

This must make the P0 a genuinely handoff-ready starting dossier while remaining `P0_GO=false`.

## 8. WP-B — Expression 5 bounded closure

The exact #4285 Expression deduction is bounded: information density/small text plus reviewer visibility.

Required actions:

1. remove the duplicate legacy `60 sec / 5 min / deep read` public route from both languages and derived report surfaces; keep one canonical `30 sec / 3 min / 15 min` route;
2. simplify and enlarge the densest overall/state-diagram labels visible to the reviewer;
3. repair A3 page 4's dense lower table and use page 3's available whitespace to improve hierarchy where useful;
4. ensure A0 page 1 and A3 page 1 are genuinely self-sufficient at fit-to-page scale;
5. ensure the first 1440×1600 report/visual HTML viewports show the urban-design thesis and the strongest relevant readiness evidence without audit-first clutter;
6. keep the existing cover unless a real defect is found;
7. regenerate all affected bilingual PDF/HTML/figure surfaces;
8. test mobile/narrow tables and avoid avoidable horizontal-scroll dependence on primary content.

Do not broaden into a full visual redesign. Strategy C is explicitly rejected unless a concrete defect forces it.

## 9. Human award protection

For each change, ask:

- does this still read as an urban-design proposition first?
- does it strengthen professional handoff rather than consultancy-style bureaucracy?
- does it preserve ordinary city, public access, AI_OFF_CITY, human authority and NO-BUILD?
- would a planning/architecture/implementation/AI-operations juror find it more credible?

Reject changes that improve machine-facing density at the cost of human clarity.

## 10. Expected file scope

Allowed substantive scope is bounded to Jerry's submission subtree.

Expected/allowed files include:

- `proposal.md`, `proposal.en.md`
- `assumptions.json`, `metrics.json`, `sources.json`
- `compliance_matrix.json`, `design_depth_matrix.json`, `standard_matrix.json` only when synchronization requires it
- `visual/assets/p0-s01-reference.json`
- `visual/assets/renewal-project-portfolio.json`
- `visual/assets/taskbook-closure.json`
- `visual/assets/design-evidence-index.json`
- at most one integrated implementation-readiness source JSON
- `assets/figures/metrics-evidence.png/.en.png`
- other fixed reviewer figure pairs only when the QA/audit proves a specific density/readability defect
- `report/proposal.html`, `report/proposal.en.html`
- `visual/index.html`, `visual/index.en.html`
- A0/A3 PDFs zh/en
- `manifest.json`, `self_check.json`, changelog and copyright/provenance only as required

Do NOT change:

- official repository scripts/workflows/rubric;
- other participants;
- geometry unless an authoritative official source actually requires a truthful correction;
- core counts/personas/scenarios/key-area selection;
- unrelated figures/assets.

Outside-scope changed files must be zero.

## 11. Build and render QA

Finish substantive source edits before formal validation.

Recommended sequence:

1. source/data/narrative readiness edits;
2. fixed reviewer-visible figure edits;
3. one complete derived build of affected bilingual HTML/PDF/figures;
4. actual rendered visual QA;
5. one batch repair and one final rebuild if needed.

Actual QA must inspect:

- gallery card/cover 16:10 crop;
- report zh/en desktop initial viewport and full-scroll spot checks;
- visual zh/en desktop initial viewport and full-scroll spot checks;
- narrow/mobile report and visual;
- every page of A0 zh/en;
- every page of A3 zh/en;
- every changed fixed reviewer figure at full size and thumbnail/fit-to-page scale;
- tofu, clipping, overlap, density, smallest meaningful text, tables, hierarchy, whitespace, bilingual parity and wrong labels.

Hard human receipts:

- `URBAN_DESIGN_FIRST_IMPRESSION=PASS`
- `HUMANISTIC_THESIS_VISIBLE=PASS`
- `P0_PROFESSIONAL_HANDOFF=PASS`
- `AUDIT_LANGUAGE_SUBORDINATE=PASS`
- `REVIEWER_VISIBLE_READINESS=PASS`

## 12. Certification

After final product bytes are frozen, use CURRENT upstream tooling:

1. refresh submission manifest;
2. run one marked `self_check_submission.py` for `JerrySkywalker`;
3. run `participant_preflight.py --skip-self-check --check-push`;
4. `git diff --check`;
5. prove outside-scope files = 0;
6. do not redundantly rerun the four gates individually unless diagnosing a failure.

If any content changes after the marked self-check, refresh/re-certify before push.

## 13. Successor PR

Create exactly one new successor PR from the new branch to current official `main`.

Suggested title:

`Jing-Zhang In Place — close participant readiness and jury readability`

Describe real design/professional improvements, not “score hacking”.

Do not initiate merge. If maintainers externally merge it, record the event and continue only as safely possible on the exact reviewed/merged state.

## 14. Exact-head trusted-review loop

Wait for exact-head CI and trusted review.

Primary machine success:

`TRUSTED_REVIEW_SCORE=100`

No identical-head rerolls.

At most **2 content-changing repair cycles total** on the same PR:

- if review identifies a specific participant-controlled Implementation/Expression defect inside this Goal, repair it, rebuild, re-QA, re-certify, push a fresh head and await fresh exact-head evidence;
- if the requested item is external-only (real rights, operator, budget, field measurement, approval, etc.), do not fabricate or pseudo-close it;
- preserve all five existing 5/5 dimensions;
- if a repair would risk a protected dimension more than it helps, do not perform it.

Interpret final vectors carefully:

- `5,5,5,5,5,5,5`: success, freeze immediately;
- `5,5,5,4,5,5,5`: Expression closed; use remaining repair only if exact Implementation deduction is participant-controlled;
- `5,5,5,5,5,5,4`: Implementation closed; use remaining repair only for a specific bounded Expression defect;
- unchanged 93: perform only evidence-backed remaining participant repair; never add generic rubric bloat.

## 15. Final independent Supervisor

After the final exact trusted outcome (including bounded repair cycles), launch exactly one independent read-only Supervisor using Sol Ultra or the configured supervisor role.

Supervisor must verify:

- exact final branch/head;
- CI state;
- trusted review is bound to that head;
- trusted vector/score;
- all five protected dimensions unchanged;
- no fabricated external closures;
- `P0_GO=false` unless real external evidence unexpectedly and legitimately exists;
- human visual receipts;
- outside-scope files zero;
- PR/merge state;
- no unauthorized merge initiated by the writer.

Supervisor must not edit product files.

## 16. Stop conditions

Do not interrupt Owner for routine choices, rendering repairs, CI waits or participant-controlled reviewer deductions.

Stop only for:

- authentication/credential requirement that cannot be completed unattended;
- destructive action outside this one PR;
- material official-rule contradiction;
- a requested truth claim that cannot safely remain assumption/unknown/HOLD;
- external service/deadline preventing further progress;
- two justified trusted repair cycles exhausted without 100.

## 17. Final owner receipt

Return at least:

```text
DISPOSITION=
PROGRAM_ID=JZ-FINAL100-AWARD-CONSOLIDATION-0830
GOAL_ID=JZ100-A03-POST4285-PARTICIPANT-CLOSURE-001

START_UPSTREAM=
BASELINE_PR=4285
BASELINE_HEAD=af75ac57f97f977f5f842dcf28a8b3263fb07e9a
FINAL_BRANCH=
FINAL_HEAD=
PR_NUMBER=
PR_URL=

READINESS_DOSSIER=
CARRIER_REGISTER=
REFERENCE_TASK_PACKET=
RACI_HANDOFF=
HAZARD_LOCKOUT_NO_GO=
OPERATIONS_RUNBOOK=
ACCEPTANCE_MATRIX=
RIGHTS_ACQUISITION_WORKFLOW=
DAY1_WEEK1_HOLD_MAP=
PUBLIC_CONTEXT_REFRESH=

DUPLICATE_READING_ROUTE_REMOVED=
REVIEWER_VISIBLE_READINESS=
A0_PAGE1_SELF_SUFFICIENT=
A3_PAGE1_SELF_SUFFICIENT=
ALL_PAGE_VISUAL_QA=
URBAN_DESIGN_FIRST_IMPRESSION=
HUMANISTIC_THESIS_VISIBLE=
P0_PROFESSIONAL_HANDOFF=
AUDIT_LANGUAGE_SUBORDINATE=

DETERMINISTIC=
SPATIAL=
VISUAL=
PROFESSIONAL=
PARTICIPANT_PREFLIGHT=
OUTSIDE_SCOPE_FILES=
GIT_DIFF_CHECK=

REMOTE_CI=
TRUSTED_REVIEW_SCORE=
TRUSTED_VECTOR=
TRUSTED_DISPOSITION=
TRUSTED_REPAIR_CYCLES=

PROTECTED_DIMENSIONS_REGRESSED=false
P0_GO=false
EXTERNAL_FACTS_FABRICATED=false
MERGE_INITIATED_BY_WRITER=false
MERGED=

FINAL_SUPERVISOR=
OWNER_ACTION_REQUIRED=
```

Proceed as one continuous unattended train. The target remains **100/100**, but truth boundaries and human urban-design quality are non-negotiable.
