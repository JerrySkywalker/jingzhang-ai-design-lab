# Owner brief

OWNER_REQUIRED=true

The current local Codex CLI can launch non-interactive ephemeral read-only sessions using the existing subscription and no API key. It did not provide a safe packet-only isolation boundary: a reviewer attempted to inspect host memory outside the neutral candidate packet. The coordinator stopped the jury and discarded all incomplete output.

No V041A local score exists, no V042/V043/V044 content was created, and no product repository or official PR was mutated. A continuation needs an Owner-approved execution environment that physically confines reviewer sessions to the fixed packet while retaining authenticated subscription use, or an explicit revision of the isolation contract.
