# Next score-loop admission contract

Do not resume the three-jury score loop until all of the following are recorded for a fresh Sandbox harness test: subscription device-auth success without API key; ephemeral non-resumed execution; valid disposable schema output; packet-only evidence references; no host-context leakage; and reviewer-specific output isolation.

Only then may a separate Goal score a neutral candidate. It must still run exactly three independent juries and may not reuse this disposable output as a candidate score.
