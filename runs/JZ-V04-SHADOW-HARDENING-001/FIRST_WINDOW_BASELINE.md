# Baseline first-window record

The trusted 77-point baseline contains all required bilingual figures, A3/A0 PDFs, and HTML artifacts. PNG signature checks and PDF first-page existence checks pass.

Its visible packet already exposes `STATUS × ACTION`, core task IDs, and `NO BUILD` material, but it has no canonical `visual/assets/ai-spatial-admission.json`. Accordingly it cannot prove the v0.4-only admission chain, 12-to-3 split, three interfaces, three delivery contracts, public-rights floor, or 100-day handoff from one reviewer-facing source.

This is a diagnostic difference, not a defect attributed retrospectively to the trusted baseline.
