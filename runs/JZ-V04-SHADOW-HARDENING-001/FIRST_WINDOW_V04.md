# v0.4 first-window record

All 18 actual Review Agent surface candidates exist and pass non-zero/signature checks. Across the proposal, report/visual HTML and canonical admission source, the harness finds:

- `STATUS × ACTION`;
- the 12-to-3 decision (`S01`, `S04`, `S07`);
- ordinary-space sufficiency and nine `NO BUILD` outcomes;
- three reversible interfaces;
- the D0–D100 handoff signal;
- `STOP / RESET`.

The visual first screen places the thesis, public-rights floor, three deep task IDs and D0–D100 handoff before the main content. The full admission record preserves all 12 task facts and their source links to existing `simulation.json`.
