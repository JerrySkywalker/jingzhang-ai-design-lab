# Morning Brief

Status: `GREEN KICKOFF / FORMAL BASELINE NOT YET REVIEW-READY`.

## A. Repository setup

The `JerrySkywalker/haidian` fork and lightweight workspace at `V:\src\haidian` exist. `origin` is the fork, `upstream` is `open-city-ai/haidian`, and work is isolated on `submission/JerrySkywalker/jingzhang-in-place`. The official Skill is installed and checked. No upstream PR exists.

## B. Formal package state

The official scaffold is present. Real v0.1 content now includes Chinese and English proposals, source/assumption registers, 12 scenarios/8 personas, C05-labelled concept geometry, five paired deterministic figures, paired proposal/visual HTML, paired A3/A0 baseline PDFs and a design-lab export hash receipt. Matrices are started. Package state intentionally remains `scaffold`; building, professional sections, precise geometry and final boards are not complete.

## C. C05 production state

Formalization strengthened the controlling invariant: a status word is not a design action. Each controlling patch now needs source, limit, trigger, users, section, responsible role, stop/go and exit/reuse. The largest newly visible weakness is not conceptual contradiction but missing professional carrier/access/user evidence. AI retains a conditional pass because three task packets alter selected room/service states and also produce honest `NO BUILD` findings.

## D. Toolchain

Python/Pillow generate paired PNGs; ReportLab generates correct-size A3/A0; official renderer generates paired report HTML; the participant builder generates paired offline visual HTML. Microsoft YaHei/Arial are system-only fonts and are not redistributed. Poppler rendering and PDF text/page checks pass at smoke level.

## E. Validation

Spatial, visual and professional-evidence gates pass. Local deterministic validation and aggregate self-check fail only on the honest scaffold state and 12 incomplete/data-gap professional depth items; dependency, path, schema, manifest-hash and allowed-file errors are cleared. Workspace/scope preflight passes. The official end-of-run contract change adds `completeness_limited_by` disclosure but retains the complete-only formal gate, so no gap was relabelled to manufacture readiness. The final peer-only delta `jingzhang-civic-stack` uses an evidence line, care ribs and three stack-yards rather than C05's unequal status-action renewal field; no direct near duplicate was found.

## F. Actual critical path

```text
status/action/section architecture
→ topology-safe geometry
→ metrics/matrices
→ five bilingual core figures
→ paired HTML/PDF
→ finalize + four review gates
→ preflight
→ Owner final lock
→ official PR
```

## G. Next Owner decisions

1. At next review, confirm or decline C05 as the final production winner after seeing the formal baseline—not merely the candidate memo.
2. Confirm the display name `京张续城 / Jing-Zhang In Place` or choose a pre-tested alternative without changing the proposition.
3. Accept the explicit portfolio position: implementation/public interest and site response outrank absolute conceptual white space.
4. Authorize any field/professional survey access available before feature freeze; absence means affected geometry stays typological.
5. On 08-20, authorize opening the official PR only if the exact-head final gates pass.
